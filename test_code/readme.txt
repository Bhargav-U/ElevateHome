Unzip the config file and put folders inside the config file in the same directory as that of the main.py

so ur test_code folder must contain lib,bin,pyvenv.cfg,main.py.where lib,bin,pyvenv.cfg are inside the config.zip

